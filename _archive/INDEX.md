# Archive — Old Code & Libraries

Code prototypes, external libraries, and earlier iterations. **Not actively used** — kept for reference or potential reuse.

## External Libraries
- **`api.congress.gov-main/`** — Congress API client library
  - Reference implementation for fetching legislative data
  - Includes Python and Java clients

## Build Artifacts & Prototypes
- **`build/`** — Build output and artifacts
  - Generated during development; regenerable from source

- **`MRA 2/`** — Earlier iteration of MRA (Member Representational Allowance) analysis
  - Replaced by current implementation
  - Kept for historical reference

---

## When to Use Archive

- **Troubleshooting API issues:** Check `api.congress.gov-main/Documentation`
- **Revisiting old analysis:** Reference `MRA 2/` for earlier approaches
- **Rebuilding:** Run build system from project root (not from this folder)

**Tip:** You can safely delete old versions from `_archive/` after confirming the current implementation works.
